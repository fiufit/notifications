import { subscriberRepository } from "@src/repositories/subscriber-repository";
import { pushNotificationRepository } from "@src/repositories/push-notification-repository";

export { subscriberRepository, pushNotificationRepository };
