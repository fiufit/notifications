version: "3.8"

services:
  notifications:
    container_name: notifications
    build:
      context: .
      dockerfile: Dockerfile
      target: base
    expose:
      - '8080'
    ports:
      - '8080:7771'
    command: npm run start
